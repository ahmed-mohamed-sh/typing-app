{
  "name": "typing-app",
  "version": "1.0.0",
  "description": "typing app",
  "scripts": {
    "build": "echo 'Building static site' && mkdir -p dist && cp index.html dist/ || exit 0"
  },
  "static": true
}